/*
 * This file is generated by jOOQ.
 */
package it.usr.web.usromniapp.domain.tables;


import it.usr.web.usromniapp.domain.Decreti;
import it.usr.web.usromniapp.domain.Indexes;
import it.usr.web.usromniapp.domain.Keys;
import it.usr.web.usromniapp.domain.tables.LTipoProc.LTipoProcPath;
import it.usr.web.usromniapp.domain.tables.Utenti.UtentiPath;
import it.usr.web.usromniapp.domain.tables.records.DelegaRecord;

import java.time.LocalDateTime;
import java.util.Arrays;
import java.util.Collection;
import java.util.List;

import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Select;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class Delega extends TableImpl<DelegaRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>decreti.delega</code>
     */
    public static final Delega DELEGA = new Delega();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<DelegaRecord> getRecordType() {
        return DelegaRecord.class;
    }

    /**
     * The column <code>decreti.delega.id_delega</code>.
     */
    public final TableField<DelegaRecord, Integer> ID_DELEGA = createField(DSL.name("id_delega"), SQLDataType.INTEGER.nullable(false).identity(true), this, "");

    /**
     * The column <code>decreti.delega.id_utente</code>.
     */
    public final TableField<DelegaRecord, Integer> ID_UTENTE = createField(DSL.name("id_utente"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>decreti.delega.id_delegante</code>.
     */
    public final TableField<DelegaRecord, Integer> ID_DELEGANTE = createField(DSL.name("id_delegante"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>decreti.delega.id_tipo_proc</code>.
     */
    public final TableField<DelegaRecord, Integer> ID_TIPO_PROC = createField(DSL.name("id_tipo_proc"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>decreti.delega.data_inizio</code>.
     */
    public final TableField<DelegaRecord, LocalDateTime> DATA_INIZIO = createField(DSL.name("data_inizio"), SQLDataType.LOCALDATETIME(0).nullable(false), this, "");

    /**
     * The column <code>decreti.delega.data_fine</code>.
     */
    public final TableField<DelegaRecord, LocalDateTime> DATA_FINE = createField(DSL.name("data_fine"), SQLDataType.LOCALDATETIME(0).defaultValue(DSL.inline("NULL", SQLDataType.LOCALDATETIME)), this, "");

    /**
     * The column <code>decreti.delega.note</code>.
     */
    public final TableField<DelegaRecord, String> NOTE = createField(DSL.name("note"), SQLDataType.VARCHAR(255).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    private Delega(Name alias, Table<DelegaRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private Delega(Name alias, Table<DelegaRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>decreti.delega</code> table reference
     */
    public Delega(String alias) {
        this(DSL.name(alias), DELEGA);
    }

    /**
     * Create an aliased <code>decreti.delega</code> table reference
     */
    public Delega(Name alias) {
        this(alias, DELEGA);
    }

    /**
     * Create a <code>decreti.delega</code> table reference
     */
    public Delega() {
        this(DSL.name("delega"), null);
    }

    public <O extends Record> Delega(Table<O> path, ForeignKey<O, DelegaRecord> childPath, InverseForeignKey<O, DelegaRecord> parentPath) {
        super(path, childPath, parentPath, DELEGA);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class DelegaPath extends Delega implements Path<DelegaRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> DelegaPath(Table<O> path, ForeignKey<O, DelegaRecord> childPath, InverseForeignKey<O, DelegaRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private DelegaPath(Name alias, Table<DelegaRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public DelegaPath as(String alias) {
            return new DelegaPath(DSL.name(alias), this);
        }

        @Override
        public DelegaPath as(Name alias) {
            return new DelegaPath(alias, this);
        }

        @Override
        public DelegaPath as(Table<?> alias) {
            return new DelegaPath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Decreti.DECRETI;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.DELEGA_FK_DELEGA_DELEGANTE_IDX, Indexes.DELEGA_FK_DELEGA_TIPOPROC_IDX, Indexes.DELEGA_FK_DELEGA_UTENTE_IDX);
    }

    @Override
    public Identity<DelegaRecord, Integer> getIdentity() {
        return (Identity<DelegaRecord, Integer>) super.getIdentity();
    }

    @Override
    public UniqueKey<DelegaRecord> getPrimaryKey() {
        return Keys.KEY_DELEGA_PRIMARY;
    }

    @Override
    public List<ForeignKey<DelegaRecord, ?>> getReferences() {
        return Arrays.asList(Keys.FK_DELEGA_DELEGANTE, Keys.FK_DELEGA_TIPOPROC, Keys.FK_DELEGA_UTENTE);
    }

    private transient UtentiPath _fkDelegaDelegante;

    /**
     * Get the implicit join path to the <code>decreti.utenti</code> table, via
     * the <code>fk_delega_delegante</code> key.
     */
    public UtentiPath fkDelegaDelegante() {
        if (_fkDelegaDelegante == null)
            _fkDelegaDelegante = new UtentiPath(this, Keys.FK_DELEGA_DELEGANTE, null);

        return _fkDelegaDelegante;
    }

    private transient LTipoProcPath _lTipoProc;

    /**
     * Get the implicit join path to the <code>decreti.l_tipo_proc</code> table.
     */
    public LTipoProcPath lTipoProc() {
        if (_lTipoProc == null)
            _lTipoProc = new LTipoProcPath(this, Keys.FK_DELEGA_TIPOPROC, null);

        return _lTipoProc;
    }

    private transient UtentiPath _fkDelegaUtente;

    /**
     * Get the implicit join path to the <code>decreti.utenti</code> table, via
     * the <code>fk_delega_utente</code> key.
     */
    public UtentiPath fkDelegaUtente() {
        if (_fkDelegaUtente == null)
            _fkDelegaUtente = new UtentiPath(this, Keys.FK_DELEGA_UTENTE, null);

        return _fkDelegaUtente;
    }

    @Override
    public Delega as(String alias) {
        return new Delega(DSL.name(alias), this);
    }

    @Override
    public Delega as(Name alias) {
        return new Delega(alias, this);
    }

    @Override
    public Delega as(Table<?> alias) {
        return new Delega(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public Delega rename(String name) {
        return new Delega(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public Delega rename(Name name) {
        return new Delega(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public Delega rename(Table<?> name) {
        return new Delega(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Delega where(Condition condition) {
        return new Delega(getQualifiedName(), aliased() ? this : null, null, condition);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Delega where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Delega where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Delega where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Delega where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Delega where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Delega where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Delega where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Delega whereExists(Select<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Delega whereNotExists(Select<?> select) {
        return where(DSL.notExists(select));
    }
}
